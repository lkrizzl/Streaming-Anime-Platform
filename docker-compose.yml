services:
  webapi:
    image: ${DOCKER_REGISTRY-}sreaming-api
    build:
      context: .
      dockerfile: WebApi/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=${DB_CONNECTION_STRING}
      - MediaService__InternalApiKey=${MEDIA_SERVICE_INTERNAL_API_KEY}
      - MediaService__BaseUrl=http://mediaservice.api:8080
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "8080:8080"
      - "8081:8081"
    depends_on:
      streaming.database:
        condition: service_healthy
    networks:
      - default
      - shared_network

  streaming.database:
    image: postgres:18
    container_name: streaming.database
    volumes:
      - streaming-db-data:/var/lib/postgresql
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  streaming-db-data:

networks:
  shared_network:
    name: streaming_shared
    external: true